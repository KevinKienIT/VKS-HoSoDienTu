# VKS ECMS — Python dependencies
# Install: pip install -r requirements.txt
# Python: 3.11.9 (tested 2026-05-04)
# Venv: PhanMem/.venv

# === PRIMARY OCR ENGINE (RapidOCR + ONNX) ===
# RapidOCR is the primary Windows OCR engine: lightweight, fast, ONNX-based.
rapidocr>=3.8.0
onnxruntime>=1.18.0

# === SECONDARY OCR ENGINE (PaddleOCR — optional) ===
# PaddleOCR remains for environments where Paddle runtime is stable.
# NOTE: PaddleOCR 3.5+ uses .predict() instead of .ocr() — API breaking change.
paddleocr>=2.7.0
paddlepaddle>=2.6.0

# === PDF PROCESSING ===
PyMuPDF>=1.24.0          # fitz — read PDF, extract page images, text extraction

# === IMAGE PREPROCESSING ===
# Used by OCR pipeline preprocessing and handwriting detection
opencv-python-headless>=4.9.0
numpy>=1.24.0
Pillow>=10.0.0

# === AI OFFLINE CLIENT ===
# Ollama Python client for local LLM (Qwen 2.5 3B/7B)
# Requires Ollama desktop: https://ollama.com/download
ollama>=0.4.0

# === DOCX EXPORT ===
# Tạo file Word (.docx) chuyên nghiệp từ OCR output
python-docx>=1.1.0

# === Catalog scanner (stdlib only — no extra deps) ===
