	 Không code, tôi muốn bạn là một chuyên gia trong lĩnh vực lên lịch trình, và kiểm soát thông tin, leader dự án
	 tôi muốn cải thiện phàn memory, luồng code, workflow trong project này ở trong thư mục bản v1 để mọi agent đèu phải tiến hành theo, : Thứ nhất trên tất cả các api hay module đều không được ghi version, chỉ được ghi ngày tạo module, ngày cập nhật mới nhất, được sửa bởi agent nào. tạo module, function là có mục đích gì, tham số truyền vào, lấy ra. không được tạo file bừa bên ngoài thư mục chính, phải tạo theo các thư mục được sắp xếp. buộc phải đánh số trước đầu các thư mục tập tin, file kế hoạch lập ra cần phải có ngày lập, trong đó ghi mục lụcd dầy đủ sửa cái gì Các agent cần đọc từ thư mục prompt thô của người dùng viết ra dạng text trong đó, suy luận đưa ra phân tích vào thẳng plan được theo của agent đầu tiên đọc được, (nếu agent phát hiện kế hoạch chưa được tạo theo câu lệnh yêu cầu của người dùng thì tạo đặt, trong câu lệnh của người dung nhất thiết phải ghi file plan, kế hoạch, map là những file nào để các agent có luồng check lại, không tự sinh, tự hiểu, mọi thứ đều phải có note lại, Agent sau đọc prompt phân tích plan,tranh luận với agent đầu tiên, ự động cập nhật
	 Cập nhập thông tin tất cả các thư mục đều phải dùng tiếng việt không dấu, viết cách nhau bởi dấu _ thay vì cach
	 Các agent được phân công và khi được bảo làm gì, phải tự biết mình là agent gì để mà làm nhiệm vụ gì. 
	 Agent leader có nhiệm vụ kiểm ra kế hoạch, thống nhất thông tin triển khai của agent đã đúng hướng tiến độ chưa, có tự nghĩ ra mỗi phương thức khác không, phương thức thêm mới có cải thiện gì không hay là đi lệch hẳn luồng phát triển của phần mềm
	 Phần mềm yêu cầu phải có giao diện đồ hoạ đẹp, như đang dùng trên trình duyệt web, từ hover, responsive, tới các sự kiện ẩn hiện tự động, phóng to với các hiệu ứng yêu cầu, tự động lưu mà không cần phải nhấn cài đặt khi sử dụng các kiểu button như kiểu của mobile
	 phần mềm được dùng cho nhiều kiểm sát viên nên có khả năng ghi nhớ được lịch trình , cách sử dụng của người dùng, giao diện chữ, kích cỡ chữ của người sử dụng
	 phần mềm có thể đọc được thông tin hồ sơ người phạm tôi, đưa ra được lý lịch trích ngang, trích dọc các thiết bị các vật chứng đi kèm với đối tượng được soi sét (một dạng project như kiểu trong các hồ sơ tuyệt mật của FBI)
	 Đưa ra được hình ảnh ở trong  các hồ sơ scan, có trích dẫn đầy đủ tất cả các thồng tin về file được trích dẫn ra. thông tin có sự mô tả chi tiết. 
	 có thể dùng AI dạng offline để phân tích hồ sơ, tổng hợp thông tin, ý kiến, suy luận, đánh giá của một trợ lý giúp cho kiểm sát viên nhanh chóng tìm được các thông tin cần thiết, hoặc khái quát quá hồ sơ một cách cực kỳ nhanh chóng và giao diện thân thiện khôgn phải giao diện cmd. 
	 một file chạy dạng exe (single instance) không cho chạy đa phiên. hãy góp ý thêm nếu thông tin tôi đưa ra còn thiếu
	 
